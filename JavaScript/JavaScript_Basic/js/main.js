'use strict';

{
  const point = {
    x: 100, 
    y: 180,
  };

// const keys = Object.keys(point);
// keys.forEach(key => {
// console.log(`Key: ${key} Value: ${point[key]}`);
// });

//複数の座標を管理したかった時
const points = [
  {x: 30, y:20}, // 0番目
  {x: 10, y:50}, // 1番目
  {x: 40, y:40}, // 2番目
];
console.log(points[1].y); // 1番目の y を取り出す
}   



